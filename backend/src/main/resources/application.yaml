spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/wtbuddy
    username: wtbuddy
    password: wtbuddy123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: 3c1628e739ecc1
    password: 300d794be59b7d
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION}

server:
  port: 8080